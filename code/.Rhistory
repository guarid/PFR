members <- read.table("/Users/hermann-kevin/Desktop/PFR/data/raw_data/members.csv", sep=",", header = TRUE)
size <- dim(members)
#Nombre de membres
size[1]
#Nombre d'utilisateurs par ville
table(members['city'])
#Nombre d'utilisateurs par ville
pie(table(members['city']))
#Nombre d'utilisateurs par ville
table(members['city'])
#Age des utilisateurs
age <- as.matrix(table(members['bd']))
#Moyenne des ages des utilisateurs
#On considère les valeurs en dessous de 0 et au dessus de 100 comme des valeurs abbérantes
# '101','102','103','105','106','107','111','112','131','144'
ag <- subset(age, select = -c('-43','-38','0','931','1030','1051') )
table(members['bd'])
#Différents genre des utilisateurs
table(members['gender'])
#Différents types d'enregistrement des utilisateurs
table(members['registered_via'])
#Différents types d'enregistrement des utilisateurs
pie(table(members['registered_via']))
table(members['registration_init_time'])
boxplot(data)
mcor <- cor(data)
mcor <- cor(members)
#Différents genre des utilisateurs
table(members['gender'])
genre<-rep(NA,size)
genre<-rep(NA,size[1])
genre
for(i in 1:size[1]){genre[i]<-ifelse(members[i,1],"female",ifelse(members[i,2],"male",ifelse(covtype[i,3],"Nan",NA)))}
genre<-as.factor(genre)
for(i in 1:size[1]){genre[i]<-ifelse(members[i,1],"female",ifelse(members[i,2],"male",ifelse(covtype[i,3],"Nan",NA)))}
genre<-rep(NA,size[1])
for(i in 1:size[1]){genre[i]<-ifelse(members[i,1],"female",ifelse(members[i,2],"male",ifelse(members[i,3],"Nan",NA)))}
for(i in 1:size[1]){genre[i]<-ifelse(members[i,1],"female",ifelse(members[i,2],"male",NA)))}
for(i in 1:size[1]){genre[i]<-ifelse(members[i,1],"female",ifelse(members[i,2],"male",NA))}
genre<-rep(NA,size[1])
for(i in 1:size[1]){genre[i]<-ifelse(members[i,1],"female",ifelse(members[i,2],"male",NA))}
genre<-as.factor(genre)
genre
members['gender', 6]
members['gender', 5]
members['gender'][5]
members['gender',0]
members['gender',1]
members[6, 'gender']
members[5, 'gender']
genre<-rep(NA,size[1])
for(i in 1:size[1]){genre[i]<-ifelse(members['gender',1],"female",ifelse(members['gender',2],"male",NA))}
X <- subset(members, select = -c('gender') )
X <- subset(members, select = -c(4) )
X
X <- subset(members, select = -c(['gender'] ) )
4
X <- subset(members, select = -c(4) )
X
mcor <- cor(X)
X <- subset(members, select = -c(1,4) )
X
mcor <- cor(X)
corrplot(abs(mcor), type="upper", order="hclust", tl.col="black", tl.srt=45)
library("corrplot", lib.loc="/Library/Frameworks/R.framework/Versions/3.3/Resources/library")
corrplot(abs(mcor), type="upper", order="hclust", tl.col="black", tl.srt=45)
members <- read.table("/Users/hermann-kevin/Desktop/PFR/data/raw_data/members.csv", sep=",", header = TRUE)
size <- dim(members)
#Nombre de membres
size[1]
#Nombre d'utilisateurs par ville
table(members['city'])
#Age des utilisateurs
age <- as.matrix(table(members['bd']))
table(members['bd'])
members[5, 'gender']
members[5, 'gender']
#Différents genre des utilisateurs
table(members['gender'])
genre<-rep(NA,size[1])
for(i in 1:size[1]){genre[i]<-ifelse(members['gender',1],"female",ifelse(members['gender',2],"male",NA))}
genre<-as.factor(genre)
X <- subset(members, select = -c(1,4) )
X
#Différents types d'enregistrement des utilisateurs
pie(table(members['registered_via']))
mcor <- cor(X)
corrplot(abs(mcor), type="upper", order="hclust", tl.col="black", tl.srt=45)
library("corrplot", lib.loc="/Library/Frameworks/R.framework/Versions/3.3/Resources/library")
corrplot(abs(mcor), type="upper", order="hclust", tl.col="black", tl.srt=45)
mcor
mcor
#Nombre d'utilisateurs par ville
table(members['city'])
members <- read.table("/Users/hermann-kevin/Desktop/PFR/data/raw_data/members.csv", sep=",", header = TRUE)
size <- dim(members)
#Nombre de membres
size[1]
#Nombre d'utilisateurs par ville
table(members['city'])
table(members['bd'])
